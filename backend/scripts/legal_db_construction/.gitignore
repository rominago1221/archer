# Build artifacts — large and regeneratable, not meant for git.
.venv/
raw_documents/
enriched_documents/
*.log
*.jsonl
*.npy

# Discovery notes we DO want to keep in git
!cn_discovery.json

# Committed corpus bundle (used by seed_legal_db_v2.py on deploy).
# Overrides root .gitignore's **/*.tar.gz exclusion.
!legal_db_v2_seed.tar.gz
