"""
Scripts package. Most files here are runnable as standalone CLI tools
(`python -m scripts.migrate_to_credits`), but a couple of them also
expose pure async functions that admin endpoints import directly
(see routes/admin_migrations_routes.py).
"""
